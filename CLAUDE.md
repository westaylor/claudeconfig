# CLAUDE.md

## Project Overview

claudeconfig — Configuration files, settings, and setup scripts for Claude Code environments.

## Structure

- `settings/` — Example CLAUDE.md templates and configuration examples
- `scripts/` — Setup scripts (e.g., `setup-windows.ps1`)

## Conventions

- `settings/example-CLAUDE.md` is a reference template, not a live config — keep examples realistic and up to date
